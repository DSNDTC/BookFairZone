server:
  port: 8097

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/stall_db?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: rootpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
