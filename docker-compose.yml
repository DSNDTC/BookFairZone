services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.3.0
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - app-network

  kafka:
    image: confluentinc/cp-kafka:7.3.0
    container_name: kafka
    ports:
      # Exposes Kafka to your host machine on port 9092
      - "9092:9092"
    depends_on:
      - zookeeper
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: 'zookeeper:2181'
      # Listeners:
      # - PLAINTEXT://kafka:29092 is for internal communication between containers
      # - PLAINTEXT_HOST://localhost:9092 is for communication from your host machine
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    networks:
      - app-network

  db:
    image: postgres:15-alpine # Use an official PostgreSQL image
    container_name: local_postgres_db
    restart: always
    environment:
      # Set environment variables for the database
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: reservationdb
    ports:
      # Map port 5432 on your host to 5432 in the container
      # This allows your app (even outside Docker) to connect via localhost:5432
      - "5432:5432"
    volumes:
      # Persist database data locally in a Docker volume
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myuser -d reservationdb"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  security-service:
    build:
      context: .
      dockerfile: security-service/Dockerfile
    container_name: security-service
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/${DB_NAME}
      SPRING_DATASOURCE_USERNAME: ${DB_USER}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      SPRING_MAIL_HOST: ${MAIL_HOST}
      SPRING_MAIL_PORT: ${MAIL_PORT}
      SPRING_MAIL_USERNAME: ${MAIL_USERNAME}
      SPRING_MAIL_PASSWORD: ${MAIL_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    ports:
      - "8082:8080"
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 3

  api-gateway:
    build:
      context: .
      dockerfile: ./api-gateway/Dockerfile
    container_name: api-gateway
    restart: always
    ports:
      - "8080:8080"
    depends_on:
      security-service:
        condition: service_healthy
      reservation-service:
        condition: service_healthy
    environment:
      SERVER_PORT: 8080
      JWT_SECRET: ${JWT_SECRET}
      SECURITY_SERVICE_URI: http://security-service:8080
      SECURITY_SERVICE_ISSUER_URI: http://security-service:8080
      RESERVATION_SERVICE_URI: http://reservation-service:8085
      STALL_SERVICE_URI: http://stall-service:8080
      USER_SERVICE_URI: http://user-service:8080
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 3

  reservation-service:
    # Build the Dockerfile located in the ./reservation-service directory
    build:
      context: .
      dockerfile: ./reservation-service/Dockerfile
    container_name: reservation-service
    restart: always
    ports:
      # Maps your host port 8085 to the container's port 8085
      - "8085:8085"
    depends_on:
      db:
        condition: service_healthy
      kafka:
        condition: service_started
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      SERVER_PORT: 8085
      SPRING_KAFKA_TOPIC_NAME: reservation.confirmed.events
      SENDGRID_API_KEY: "YOUR_REAL_SENDGRID_API_KEY"
      SENDGRID_FROM_EMAIL: "your-verified-sender@example.com"
      DATABASE_URL: "postgresql://myuser:mysecretpassword@db:5432/reservationdb"
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: reservationdb
      DB_USERNAME: myuser
      DB_PASSWORD: mysecretpassword
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8085/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 3

  mysql:
    image: mysql:8.0
    container_name: stall_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: stall_db
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  stall-service:
    build:
      context: .
      dockerfile: ./stall-service/Dockerfile
    container_name: stall_service
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/stall_db?useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: rootpass
    ports:
      - "8086:8080"
    networks:
      - app-network

#  email-service:
#    build:
#      context: .
#      dockerfile: email-service/Dockerfile
#    container_name: email-service
#    ports:
#      - "8081:8081"
#    depends_on:
#      kafka:
#        condition: service_started
#    environment:
#      # This OVERRIDES the value in application.properties
#      # It correctly points to the internal Kafka listener
#      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
#      # Your application properties
#      SERVER_PORT: 8081
#      SPRING_KAFKA_TOPIC_NAME: reservation.confirmed.events
#
#  notification-service:
#    build:
#      context: .
#      dockerfile: notification-service/Dockerfile
#    container_name: notification-service
#    ports:
#      # Maps your host port 8083 to the container's port 8083
#      - "8083:8083"
#    depends_on:
#      kafka:
#        condition: service_started
#    environment:
#      # This OVERRIDES the value in application.properties
#      # It correctly points to the internal Kafka listener
#      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
#      # Your application properties
#      SERVER_PORT: 8083
#      SPRING_KAFKA_TOPIC_NAME: reservation.confirmed.events
#      # Placeholders for your API keys
#      SENDGRID_API_KEY: "YOUR_REAL_SENDGRID_API_KEY"
#      SENDGRID_FROM_EMAIL: "your-verified-sender@example.com"

  kafka-consumer:
    image: confluentinc/cp-kafka:7.3.0
    container_name: kafka-consumer
    depends_on:
      - kafka
      - reservation-service
    # This command block is now corrected
    command: >
      sh -c "
        echo 'Waiting for Kafka to be ready...'
        cub kafka-ready -b kafka:29092 1 60
        echo 'Kafka is ready!'
        
        # This is now on a single line to avoid shell errors
        kafka-console-consumer --bootstrap-server kafka:29092 --topic reservation.confirmed.events --from-beginning
      "
    networks:
      - app-network

  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: kafka-ui
    ports:
      - "8080:8080"
    depends_on:
      - kafka
      - zookeeper
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
      KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper:2181
    networks:
      - app-network

  # Define the network for services to communicate
networks:
  app-network:
    driver: bridge

# Define the volume for persistent data
volumes:
  postgres_data:
    driver: local
  mysql-data: